const { PrismaClient } = require('@prisma/client'); const bcrypt = require('bcryptjs'); const prisma = new PrismaClient(); async function main() { const hashedPassword = await bcrypt.hash('LifeLens123', 12); await prisma.user.upsert({ where: { email: 'demo@lifelens.ai' }, update: { password: hashedPassword }, create: { email: 'demo@lifelens.ai', name: 'Demo User', password: hashedPassword } }); console.log('Demo user seeded.'); } main().catch(console.error).finally(() => prisma.$disconnect());
